version: 2.1

description: >
  Generate a short-lived GitHub App installation token for use in CI pipelines.
  Replaces long-lived personal access tokens with scoped, ephemeral tokens.

display:
  source_url: "https://github.com/Tyba-Energy/github-app-token-orb"

orbs:
  jq: circleci/jq@3.0.2
